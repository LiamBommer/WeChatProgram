<!--pages/memberList/memberList.wxml-->
<view class='memberList-container'>
    
    <checkbox-group bindchange="TaskMememberChange">
      <!-- 标题 -->
      <view class='title'>任务负责人</view>
      <!-- 列表项 -->
      <view class='item'>
            <view class='item-one'>
              <image class='item-icon' src='{{principalIicon}}'></image>
              <view class='item-text'>{{principalName}}</view>
            </view>
            <checkbox class='item-checkbox' value="{{principalIndex}}" checked='{{principalChecked}}'/>
      </view>

      <!-- 标题 -->
      <view class='title'>任务成员</view>
      <!-- 列表项 -->
      <block wx:for="{{TaskMemember}}">
        <view class='item'>
            <view class='item-one'>
              <image class='item-icon' src='{{item.icon}}'></image>
              <view class='item-text'>{{item.name}}</view>
            </view>
            <checkbox class='item-checkbox' value="{{item.index}}" checked='{{item.checked}}'/>
        </view>
      </block>
    </checkbox-group>

    <checkbox-group bindchange="ProjectMememberChange">
      <!-- 标题 -->
      <view class='title'>项目成员</view>
      <!-- 列表项 -->
      <block wx:for="{{ProjectMemember}}">
        <view class='item'>
            <view class='item-one'>
              <image class='item-icon' src='{{item.icon}}'></image>
              <view class='item-text'>{{item.name}}</view>
            </view>
            <checkbox class='item-checkbox' value="{{item.index}}" checked='{{item.checked}}'/>
        </view>
      </block>
    </checkbox-group>
    
    <view class='button'>
      <button class='addMember' bindtap='save'>完成</button>
    </view>

</view>
